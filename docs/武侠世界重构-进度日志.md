# 江湖志·武侠世界重构 进度日志

> 配套蓝图：`docs/武侠世界重构规划-2026-08-31.md`（实施总纲，施工期间与本文档同步维护）。
> 用途：跨会话进度交接。每阶段完成后更新「已完成」与「下一步」，断言变化登记原因。
> 最后更新：2026-09-01 14:30（**W8 任务重启完成，武侠世界重构全阶段收官**）

---

## 一、总进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| W0 地基 | ✅ 完成 | 基线 22 断言 0 FAIL |
| W1 气候地图 | ✅ 完成 | 气候场/河源流汇/湖泊/树密度=湿度 |
| W2 唐制城池 | ✅ 完成 | 61×61 市坊分离/棋盘路网/坊墙/城门楼/city_info v2 |
| W3 门派领地 | ✅ 完成 | 5 派气候位选址/主殿 accent/界碑环/后山禁地，2026-09-01 |
| W4 村镇+NPC驻留 | ✅ 完成 | 村镇 v2 三模板/渡亭/NPC 驻留制/任务冻结接线，2026-09-01 |
| W5 桥梁重构 | ✅ 完成 | 四门官道/石拱桥 prop 全覆盖/桥位审计+目检，2026-09-01 |
| W6 碰撞治理 | ✅ 完成 | 可行域分区/散石聚簇/走廊 2×2/is_walkable+目检，2026-09-01 |
| W7 全量回归+终验 | ✅ 完成 | 102 断言全绿+14 机位走查+legacy 清删，2026-09-01 |
| W8 任务重启 | ✅ **完成（103 断言全绿+任务链冒烟）** | 2026-09-01，详见 §二·I。**全阶段收官** |

---

## 二·I、已完成（W8 任务重启，2026-09-01）——**全阶段收官**

**改动文件**：`world_features.gd`（quests_disabled→false + WORLD_RULES_VERSION 1→2）、`tools/probe_world_regress.gd`（quest 段采 story_started，poll 主线启动防同帧竞态）、`tools/regress_world.py`（quest 组断言规则 v2 + CURRENT_STAGE=W8）。**结果：103 断言 103 PASS 0 FAIL + 任务链端到端冒烟通过。**

### 实现要点
1. **重启即恢复**：冻结接线全走开关（W4 纪律"删除为 0"兑现）——`quests_disabled=false` 后四处自动恢复：main_story `_start_when_ready`（主1「醒来」3s 后自动启动）、quest_system `refresh_available_quests`（告示板发布 4 条）、quest_log_hud 正常列表、mob_spawner `spawn_story_camp` 解禁（主线推进到主2 才实际调用）。
2. **断言规则 v2 登记**（§10.3 纪律：规则变更→登记+升 `WORLD_RULES_VERSION=2`，禁止改断言凑绿——本次是冻结期临时语义的正式终止）：
   - `quest_available_zero`（冻结期=0）→ **`quest_available_positive`（available≥1，since=W8）**——实测 4 条；
   - 新增 **`story_started`**（since=W8）：main_story.node_index≥0 或 story_stage>0，探针 poll 至多 3s（探针 4.5s 采样 vs 主线 3.2s 启动，同帧竞态防护）；
   - `quest_active/pending_story/completed_zero`、`story_camps_zero` 保留 since=W4——重启初态语义不变（玩家未接取/主1 无委托字段/主2 未开始）。
3. **任务链端到端冒烟**（临时探针，验证后已删除）：t=11s 时 `dialog_open=true`（石伯主1 对话弹出）、`story_node=0`、`available=4`、追踪器显示「主线·醒来」待接取金卡——主线/告示板/对话/追踪四链路全通。
4. **npc_static 保持 true**：驻留制是重构核心成果（§0 痛点 6"NPC 到处游走"的治理目标）；巡游 legs 代码保留在 npc_spawner，将来若要作息巡游需同步把 `npc_anchor_dist_le3` 断言改为日程感知语义（登记制）。

### W8 基线数据锚点
available=4 / story_node=0 / story_camps=0 / 其余锚点与 W7 完全一致（世界侧零改动）。

### 🏁 收官备注
- 重构规划 W0~W8 全部落地：气候驱动群系、唐制城池、五派领地、村镇三模板+NPC 驻留、官道石拱桥、可行域政策、任务系统（重启态）。
- 遗留观感专项（非阻塞，候选）：湖缘锯齿碎水、群系交界地坪拼色、山门牌坊 prop（W3 deviation）。
- 冒烟单纯人工项仍开放：清一座怪营+存活 10 分钟观察 respawn（战斗/时间实测）。

---

## 二·H、已完成（W7 全量回归+终验，2026-09-01）

**改动性质**：无新特性——终验矩阵（102 断言×2 轮）+ 14 机位截图走查 + legacy 旧路径清删。**删后复跑回归全绿，世界零语义变化。**

### 实现要点
1. **终验矩阵**：`CURRENT_STAGE="W7"` 跑全量 → 102 PASS / 0 PENDING / 0 FAIL（历史 PENDING 已在 W1 河流改道后全部转正）。
2. **legacy 清删**（规划 §9"旧函数保留至 W7 终验后删除"兑现，净删 ~370 行）：
   - `world_generator.gd`：`_generate_city_legacy`（92 行）、legacy `_generate_towns` 主体+`TOWN_TEMPLATES` 常量、`_generate_single_town`+`_try_place_town_building`（75 行）、`CITY_HALF=22` 常量（city_half 初值直置 30，`_generate_rivers` 预设改读 `WorldData.CITY_V2["half"]`）；
   - `texture_generator.gd`：`_compose_big_building_legacy`（西式旧拼装，141 行）+ 孤儿助手 `_fill_logs`；
   - **FLAG 门简化**：`_ready` 六处 `if WorldFeatures.FLAG[...]` 改直呼（新系统即唯一路径）；`_generate_city_v2→_generate_city`、`_generate_towns_v2→_generate_towns` 转正（调用点零改动）；"旧 16px 树瓦分支"前阶段已清（TileSet 无 4/8/9），本次核实无残留；
   - `world_features.gd`：世界生成 FLAG（tang_city/sect_territories/town_v2/bridge_prop/walkability_policy）转为状态记录；仍生效开关=npc_static（W8 可切回巡游，legs 代码保留）、quests_disabled（W8 重启入口）；
   - 删死工具：`tools/probe_biome_shots.gd`+`run_biome_shots.py`（旧 Voronoi 截图探针，W1 起失效，W4-W6 已各自建新探针）。
3. **14 机位截图走查**（新工具 `probe_w7_shots.gd`+`run_w7_shots.py`，图入库 `docs/shots/w7_*.png`）：城内 3（广场/府衙/西市）+ 四门 4（门楼/守卫/官道）+ 桥 4（南门官道桥群/西门干流桥/窄桥/宽桥）+ 领地 2（青峰/幽冥）+ 渡口村 1。目检结论：市坊分离/棋盘路网/城门楼/行肆/主殿 accent/界碑环/驻留 NPC/石拱桥望柱栏板全部在位，观感整体协调。
4. **观感欠账处置**（W6 移交清单逐项）：
   - ① 官道桥跨宽水带碎段 → **确认存在，登记保留**。根因=湖缘噪声锯齿（W1 `_lake_surface_dist` 噪声缘+过渡带抖动），桥本体观感好、语义断言全绿；修复需动水缘生成→全图坐标重滚，非终验阶段可承受，留观感专项（可选，W8 后）；
   - ② 群系交界地坪拼色 → 确认存在（城东南角/西市外），基线行为（W2 地坪逐格按群系取色），登记保留；
   - ③ castle POI 贴镇 → 14 机位未发现（东门外为林带），W5 避让 margin 生效，**可关闭**；
   - ④ 雪原树密度偏高/染色偏绿 → 幽冥机位雪原树冷色调、密度正常，W6 已治，**可关闭**。
5. **冒烟单（规划 §10.4）自动映射**：①出生→官道进城过桥 / ②四门各走 / ③两市逛铺 / ⑤北门官道+界碑环赴剑宗 / ⑥五领地访问 → 全由 reach 洪泛+四门 BFS+door 可达+sect 断言组覆盖 ✅；④逐坊房间距 → rooms_gap 断言+机位 0/1/6 目检 ✅；⑦雪原/沙漠/竹林观感 → biome 断言+机位 12（雪原）✅；⑧清怪营+存活 10 分钟 respawn → **纯人工项**（战斗/时间实测，mob 断言已保营地在 WILD）。
6. **deviation 登记**：14 机位中"窄/宽/水关桥"的"水关桥"按 W5 deviation（城内无河）以"南门官道桥群"机位替代；渡口村机位视野未含渡亭本体（ferry_pavilion 断言 PASS+W4 机位已验，不算缺陷）。

### W7 基线数据锚点
与 W6 完全一致（世界零语义变化）：102 断言；reach=92716；桥 prop 62 座 297/297；官道 4×96（s/w 过河 32/26）；NPC 66；镇 7；sects 5 派同锚点。

### ⚠️ W8 前须知（任务重启）
- 逐 FLAG 打开：`quests_disabled`→false（main_story/quest_system/quest_log_hud/mob_spawner 四处接线自动恢复）+ `npc_static` 若要巡游改 false（legs 配置保留在 npc_spawner）；**每开一个跑全量回归**（ quests 组断言在冻结期要求全 0，重启后需按 §10.3 登记规则变更并升 `WORLD_RULES_VERSION`）。
- 观感专项候选（非阻塞）：湖缘锯齿平滑（欠账①）/ 地坪拼色（欠账②）/ 山门牌坊 prop（W3 deviation，界碑现为无碰撞标记）。
- legacy 删除后无回滚分支：任何世界生成改动直接改现行函数+重跑全量回归。

---

## 二·G、已完成（W6 碰撞治理，2026-09-01）

**改动文件**：`world_generator.gd`（_tile_zone/is_walkable/_build_wild_rock_clusters/_ensure_corridor_width/_restore_official_roads + _biome_decor_tile 散石查表 + _ready 接线 + _force_spawn_poi 兜底补官道避让）、`texture_generator.gd`（_plaster_plain，_mini_house plaster 净化）、`world_features.gd`（walkability_policy=true）、探针（§11 walk6）+回归工具。**结果：102 断言 102 PASS 0 FAIL。**

### 实现要点
1. **分区 `_tile_zone`**：ROAD=路/桥/石板/雪径（1/17/35/42）；FARM=农田/湿田/雪田（16/33/41）；SETTLEMENT=城/镇/领地（复用 is_in_settlement 语义）；其余 WILD。
2. **`is_walkable(tx,ty)` 统一查询口**：无碰撞瓦片（39/40/43 含于 collision_tiles）即可行——怪物 AI/NPC/探针的唯一可行查询（规划 §6"禁止各自再猜"）。
3. **散石聚簇 `_build_wild_rock_clusters`**：desert/snow 群系散石不再全图噪声 singles——`_rock_cells` 表只收**山缘格（mountain 群系 8 邻含非山）向 WILD 侧 3 格带**、噪声带状出露（~22%，连续性天然成带），且避净空/城圈/镇圈/领地圈+2/官道走廊；总量 cap 2200。mountain 群系崖壁成势不动（山体语义）。`_biome_decor_tile` 的 desert r>0.94 / snow r>0.83 分支改查表。
4. **走廊宽度感知 `_ensure_corridor_width`**：2×2 块洪泛（块锚 4 格全通行才扩展，起点=出生点附近首块）→"1 格可达但 2×2 不可达"的窄喉口袋按 4 连分组；**WILD 组 ≥20 格**才在缝处开 2×2（_carve_cell：水→桥 17/阻挡→channel），carve 总量限 40；聚落内组不修。基线 reach2 覆盖 83935 / reach1 92716 = 90.5%。
5. **官道复原重铺 `_restore_official_roads`**（重要教训）：旧版 POI 铺地（"门派"POI 山环写 3、城镇 POI 等）会**覆盖官道格**——POI 铺地后按登记 cells 重放铺设逻辑（水→17/崖→channel/其余→path，39 让位）。治因：`_force_spawn_poi` 的 terrain-only 最终兜底补 `_near_official_road(16)` 避让。
6. **小屋 plaster 净化**（规划 §8）：`_mini_house` 的白灰墙从素材包裁切 → `_plaster_plain` 纯色+异或哈希点噪（±4%）；house_town/cottage/temple 三 PNG 删旧重生成。
7. **deviation 登记**：
   - SETTLEMENT 可行域承诺范围=城圈内圈/镇圈 dist<13/**领地核心 21×21**（选址"21×21 水≤4"同范围）——领地外环（cheby 12~r）是势力范围含自然山环屏障（W3 feature）；禁地崖带=HOLY_GROUND 豁免；领地核心**景观水**（≤4 格）豁免（湖畔/绿洲派依水建派）；豁免登记制碰撞 {39 建筑,40 城墙,43 坊墙}。
   - "岩石贴山 3 格"断言阈值 70%（噪声带状出露天然越过 3 格边界的占比 ~25%）；"2~5 连片"以孤立 singles 占比 ≤30% 代理（实测 25%）。
8. **教训**：
   - 探针分区采样范围必须与生成承诺一致——城圈采样含外扩 2 格会把城外自然山体算违例；领地采样含外环会把 W3 山环屏障算违例。
   - **POI 铺地覆盖官道**是跨阶段覆写的又一实例（§0 痛点 4 同款）——登记 cells + 复原重铺是"规划层可写"纪律的通用兜底手法，W7 清旧路径时留意保留。
   - 雪原散石密度曾达 17%（r>0.83）——查表后聚簇总量受 cap 控制。

### W6 基线数据锚点
zone_checked=9689 零固体碰撞违例（sect 景观水豁免 2 格）；散石 28（step2 采样）贴山 78%/孤立 25%；reach2 覆盖 83935/92716=90.5%；reach=92716；prop 62 座 297/297；其余锚点同 W5。

### ⚠️ W7 前须知
- 旧路径函数删除清单（净删 ~600 行）：legacy 城 `_generate_city_legacy`、legacy 镇 `_generate_single_town`+TOWN_TEMPLATES、`_compose_big_building_legacy`、旧 16px 树瓦分支等——删前重读 §10.4 冒烟单。
- 观感欠账清单（W7 视觉轮）：① 官道桥跨宽水带碎段；② 群系交界地坪拼色；③ castle POI 贴镇；④ 雪原树密度偏高/染色偏绿（W1 遗留）。
- 目检已完成（tools/vshot_w6_*.png 2 机位：山雪过渡散石带/幽冥雪原腹地）。
- 断言总数 102；W7 终验矩阵=全量回归 + 9+ 机位走查 + 手动冒烟单（§10.4）。

## 二·F、已完成（W5 桥梁重构，2026-09-01）

**改动文件**：`world_generator.gd`（_generate_official_roads/_place_bridge_props/_make_bridge_prop/_near_official_road + _ready 接线）、`texture_generator.gd`（get_bridge_texture/_bridge_stone 静态缓存）、`world_features.gd`（bridge_prop=true）、探针（§10 桥审计）+回归工具。**结果：98 断言 98 PASS 0 FAIL。**

### 实现要点
1. **四门官道**（宽 2，≤48 格直线外铺）：过河段写 17（桥语义）；遇崖**穿崖开路**（channel 材质，同 _carve_cell 纪律——实测城周环山曾把 n/s/w 三门官道秒断成 2~10 格）；遇建筑占格 39/边界即停。`official_roads` 登记 {gate, cells, bridge_cells}。基线：4×96 格，s/w 两门过河（32/26 cells）。
2. **石拱桥 prop 统一铺贴**（`_place_bridge_props`，_ensure_connectivity 之后）：全图扫描 17 → 4 连通洪泛整段 → 矩形判定：
   - 矩形桥段：一座 prop，长边=跨河方向，沿长轴两端各外扩 1 格（桥端石阶落岸上）；
   - L 形（修补桥转弯）：分解为直段（先横后纵），阶梯形残余第三轮按原连通块兜底（允许同贴图重叠）；
   - 单格 17（修补转角）豁免 prop。
   - **z=1 关键**：World y_sort 下北半球桥 prop 若 z=0 会被 TileMap(y=0) 盖住——z1 垫在实体 z2 之下、TileMap z0 之上。
3. **贴图 `_bridge_stone(W,H,dir)`**（static+缓存任意尺寸）：条石砌块（异或哈希明度，§8 规范）+拱冠提亮/缘石压暗+两端 6px 石阶+拱腹阴影弧带+两侧 3px 栏板+望柱（每 8px 柱头）。可通行语义仍 tile 17——外观与语义彻底分离 ✓。
4. **POI 避让官道走廊**（`_near_official_road` margin=16，_try_spawn_poi + force 兜底两路）——防 POI 铺地压断官道。
5. **审计断言**（bridge 组，since=W5）：prop ≥8 座；17 格 prop 覆盖 100%（单格豁免）；∀桥侧有水；官道 4 条 len≥20；∃官道过河（s/w 门达标）。
6. **deviation 登记**：
   - 规划 §5.2 规则 2（"城镇主街朝河端点隔河有目标→配桥"）简化：渡口村配渡亭（W4 已做），跨河接驳交官道桥+连通性修补——镇选址 cheby≤13 无水使该场景罕见。
   - "宽度=路宽+2"以 prop 外扩表达为"长度方向 +2 格（石阶落岸）"，宽度保持 17 段实际宽（官道桥 2 格宽双行并排，栏板内收）——4 格宽 prop 会悬空压水缘树影，实测 2 格宽+栏板观感协调。
   - 城内水关不适用（city_interior_dry=0，城内无河）。
7. **教训**：
   - 官道穿崖必须显式开路——城址 (75,0) 周边环山，直线官道不穿崖则三门寸步难行。
   - 17 段分解要备"阶梯形"兜底——修补桥的 L/阶梯连通块单靠两轮直段分解会有残余格。
   - 桥 prop 的 y_sort 陷阱（z=0 被北半球 TileMap 盖住）已写进注释。

### W5 基线数据锚点
官道 4×96（s 过河 32 格、w 过河 26 格）；石拱桥 prop 60 座（17 格 300 全覆盖，单格豁免 141）；桥侧有水 100%；reach=90249；其余锚点同 W4。

### ⚠️ W6 前须知
- 桥 prop 是纯视觉（z1 无碰撞），W6 可行域政策把 ROAD 分区纳入时注意：桥格（17）已无碰撞，勿重复处理。
- 官道 corridor 已进 POI 避让；W6 岩石聚簇（贴山 3 格）注意别把岩石撒在官道上（ROAD 禁碰撞装饰）。
- 观感欠账（W7 视觉轮）：① 官道桥群跨宽水带的碎段观感（语义正确）；② 群系交界地坪拼色；③ castle POI 贴镇。
- 目检已完成（tools/vshot_w5_*.png 3 机位：南门官道桥群/西门官道桥群/干流石拱桥）。

## 二·E、已完成（W4 村镇+NPC驻留，2026-09-01）

**改动文件**：`world_generator.gd`（_generate_towns_v2/_generate_single_town_v2/_place_town_building/_town_anchor_ok/_place_ferry/_find_bank_spot/_water_in_band/_area_has_cliff + sect npc_anchors）、`world_data.gd`（NPC_JOBS 补货郎）、`texture_generator.gd`（ferry 渡亭 prop）、`Main.gd`（_ensure_textures 补 ferry 校验）、`npc_spawner.gd`（驻留制重写）、`main_story.gd`/`quest_system.gd`/`quest_log_hud.gd`/`mob_spawner.gd`/`game_manager.gd`（任务冻结）、`world_features.gd`（town_v2/npc_static 开启）、探针+回归工具。**结果：93 断言 93 PASS 0 FAIL。**

### 实现要点
1. **村镇 v2 三模板**（TOWN_TEMPLATES_V2 表驱动）：farm（祠堂+农舍×2+铁匠或货郎+L 形农带 2 宽）、market（祠堂+铁匠+货郎+骡马店 tavern+石板广场）、ferry（祠堂+农舍×2+货郎+渡亭）。town_info[Vector2i 中心] = {template, center, half, buildings{key:{kind,anchor,fp,door_px,job}}, ferry_axis?}，**buildings.key 与 NPC_JOBS.building 对齐=落位登记制**。
2. **选址硬规则**：村镇间 ≥24 欧氏（严于规划 24 语义）；距城保持 46 欧氏（deviation：严于规划 ≥34）；**竹林排除**（deviation：规划允许镇落竹林，但竹林带稀缺且为古刹指定气候位，8 镇铺开会挤占——1047/1200 attempts 死于 climate+town 实测后让位领地）；**崖带排除**（cheby≤10 内有 3/7 → 曾产出"1 建筑空镇"）；水规则：建成区 cheby≤13 无水，ferry 需外带 cheby 14~22 有水。
3. **门前小径预检 `_town_anchor_ok`（重要教训）**：dense 摆放时**后放建筑的 footprint(39) 会打断先放建筑的 `_carve_door_path`**（carve 只铺空地，遇 39 跳过 → door 与主路留断点）→ 首轮回归 9 FAIL（house/smithy door 不可达）。预检 door 列（door 行→主路）禁穿 39/水/崖后才接受锚点。
4. **祠堂回退**：山缘镇 7×5 temple 摆不下时回退 hut 小筑祠堂（村正岗位必须落位，断言兜底）。
5. **渡亭 prop**（fp 2×2，36×40 开放式小筑：石台基+四木柱+茅草攒尖顶，`_ferry_pavilion_img`）——deviation：尺寸过小不走 `_compose_big_building`，纯手绘；**_find_bank_spot 三重约束**：2×2 干置+外邻 1 格贴水+**亭门行（正南）必须干燥**（门格落水会断 BFS 与岗位落位）。`ferry_axis{road_end,dir}` 登记官道终点朝水方向，供 W5 渡口配桥。
6. **⚠️ 管线坑**：新 prop PNG 必须同步加入 `Main._ensure_textures` 的逐 kind 校验清单——否则 `generate_city_props` 永不触发（ferry.png 首跑缺失警告实测）。
7. **NPC 驻留制（npc_static）**：城内 15 人（原 11 配置+四门守卫×4，static 时全天 idle 驻首段岗位，原 legs 配置保留可回滚）；村镇 NPC 按 town_info 岗位落位（door_px + NPC_JOBS.offset，**禁止自算坐标**；特例 job_anchor_px=渡夫站官道终点干格，亭外临水不可站人）；领地 3 人/派（长老+弟子甲@主殿门、弟子乙@演武场，sect_info.npc_anchors 登记 hall/arena px）。全部 set_meta(anchor_px/anchor_ref) 供探针。
8. **任务冻结接线**（quests_disabled 原为 true，本次接线）：main_story._start_when_ready 早退；quest_system.refresh_available_quests 早退（零发布）；quest_log_hud 空态"江湖暂无委托"；mob_spawner.spawn_story_camp 拒绝调用（双保险）；GameManager.feature_flags 镜像。EncounterSystem 现有 20 奇遇全为纯风味（无任务型），无需改动。
9. **deviation：NPC 总量 ≤45 为规划笔误**（12+9×4.3+5×3≈77，规划自相矛盾）——实测 66（城15+镇31+领地15），全 idle 无寻路开销，性能可控；断言上限放到 90。
10. **教训（探针）**：town_centers 存 Vector2 而 town_info 键为 Vector2i——字典取键必须显式转换（静默取不到返回 {} 假数据）。

### W4 基线数据锚点
7 镇：market(110,102)/farm(-54,70)/market(-109,98)/ferry(-51,44)/farm(-54,115)/market(-103,72)/farm(-130,85)（随种子固定，详见 regress_world_data.json towns）；NPC 66（static 56，锚距全 ≤3）；任务全口径 0；河距镇 min 21.8；reach=90105；sects 5 派同 W3 锚点。

### ⚠️ W5 前须知
- 桥位规则输入已备好：`town_info[t]["ferry_axis"]`（渡口官道终点+朝水方向）；官道=城四门外接驳（W2），河交叉必有桥。
- ferry.png 已入 `_ensure_textures` 清单；新 prop 记得同步加清单。
- ~~目检截图欠账~~ **W4 目检已完成**（tools/vshot_w4_*.png 6 机位：城广场/西市/渡口村/农耕村/市镇/剑宗）：城 15+镇 31+领地 15 NPC 全部按 door_px 登记位在岗；渡亭/农带/骡马店/演武场弟子乙在位。**新增观感欠账（W7 视觉轮）**：① 群系交界处城内/领地地坪色块拼接明显（W2 地坪逐格按群系取色所致，基线行为）；② castle 系 POI 可能贴镇旁（POI 撒点避让只避城/领地，未避镇净空）。

## 二·D、已完成（W3 门派领地，2026-09-01）

**改动文件**：`world_generator.gd`（_generate_sect_territories/_sect_climate_match/_build_sect_territory/_near_sect_territory/_near_river_band + is_in_settlement/_load_poi_chunks/_try_spawn_poi/_force_spawn_poi 接入）、`world_data.gd`（SECT_HALL_KIND）、`world_features.gd`（sect_territories=true）、`texture_generator.gd`（5 主殿 kind + tile 44 界碑）、`tileset_generator.gd`、`Main.gd`、`minimap_hud.gd`、回归工具。**结果：59 断言 59 PASS 0 FAIL。**

### 实现要点
1. **5 派落位**（sect_info：center/radius/hall/fp/style/clan）：气候位匹配（temperate_mountain=mountain+temp>0.28；desert_oasis=desert（**deviation**：河畔带实测沙漠河段过稀，绿洲改由领地自建水井/药圃表达）；bamboo_edge；lakeside_bamboo=湖 24 内+温和群系；snow_black_rock）+ 距城/镇/领地**切比雪夫缘距** + 核心 21×21 水格 ≤4。
2. **选址定向搜索**（教训）：随机撒点命中率过低 → **沙漠纬度带定向**（y 110~160）+ **湖锚定朝世界中心定向 ±90°**（两湖偏居东西缘，向外锚必越界）；1200 attempts。**失败原因统计 dbg 插桩保留**（print attempts_dbg），未来调参先看它。
3. **建设组**：核心清场 25×25 → 主殿 hall_xx（fp 7×5/6×4，accent=门派主旗色，hall 结构=temple/manor）→ 弟子舍 hut×2 → 演武场石板 7×4（药王谷加药圃 6×3）→ **界碑环 tile 44**（四角必放+每边中段每 6 格）→ 后山禁地崖带（雪原派用雪崖 7）→ 净空抑树。
4. **tile 44 界碑（灰白石碑，无碰撞装饰层）**——**deviation**：规划"tile 14 含浅碰撞"实测碰撞环会被连通性修补切开（穿环开路把碑替换成通道），改无碰撞标记后稳定；"浅碰撞"语义留 W7 视觉轮 prop 化。
5. **主殿 accent 机制**：BIG_BUILDING_DEFS 新增 hall_qf/ts/gc/yw/ym（accent=蓝/红/金/药绿/黑），复用 _compose_big_building；**deviation**：STYLE 字典完整系统（roof/wall/ridge_gold/banner）推迟，先用 accent 表达主色。
6. **ClanSimulator 名册**：clans .tres（少林/武当等 8 派）与 5 领地**名称不同**——sect_info["clan"] 暂存字符串关联，.tres 增补/接线留 W4/W8（clan_generator 为 @tool 编辑器生成）。
7. **生态接入**：is_in_settlement 含领地（怪禁入）、_load_poi_chunks 强载领地 chunk、POI 撒点避让（_try/_force 两路 + 兜底全扫描，`_near_sect_territory(margin=16)`）。
8. **教训（重要）**：
   - **界碑环被"覆盖"的元凶排查链**：连通性 carve（碰撞环挡路）→ POI 铺地（margin 不足）→ **最终=领地角越出世界边界带（R-5 起边界水覆盖界碑）**。选址约束=8 断言采样点 length ≤ R-6；非采样碑位越界则铺设跳过。`ring_bad_at_build` 回读诊断保留在 sect_info。
   - 探针滑窗方向 bug：边中点 `(0,±r)` 的 `signi(p.x)=0` → 切向向量 (0,0) 永远查自身——边方向必须按"横边/竖边"显式分支。

### W3 基线数据锚点
5 派：青峰(-124,-26)r28 / 铁砂(沙漠)r24 / 古刹(145,3)r26 / 药王(湖畔)r24 / 幽冥(雪原)r28（坐标随种子固定，详见 regress_world_data.json sects）；界碑环全 8/8；reach=90583。

### ⚠️ W4 前须知
- NPC_JOBS/sect_info["clan"] 接线：领地弟子/长老 NPC 挂 hall door_px（同 door_px 落位制）。
- 目检截图仍未做（探针重写欠账，W4/W7 前必须补）。

---

## 二·C、已完成（W2 唐制城池，2026-09-01）

**改动文件**：`world_generator.gd`（_generate_city_v2 + city_half）、`world_data.gd`（CITY_V2 施工修订）、`world_features.gd`（tang_city=true）、`texture_generator.gd`（坊墙43+城门楼）、`tileset_generator.gd`（43+collision）、`Main.gd`/`minimap_hud.gd`（43 注册）、回归工具。**结果：33 断言 33 PASS 0 FAIL。**

### 实现要点
1. **FLAG 分流**：`_generate_city` → `_generate_city_v2`（tang_city）/ `_generate_city_legacy`（旧路径保留可回滚）。**`city_half` 运行时变量**（v2=30/legacy=22）替代 CITY_HALF 常量——河斥力/湖泊避城/is_in_settlement/探针全部读它；⚠️ **河先于城生成，city_half 必须在 _generate_rivers 开头预设**。
2. **城布局**（half=30，61×61）：城墙±30+四门豁口4格（对齐主街）；主街宽4（x,y∈[-2,1]全贯）；次街宽2（横 y 14..15、纵 x 14..15）；4 坊（官署/寺观/西/东，坊墙 tile **43** 圈+中横中纵巷+坊门2格+门外接引）；东西两市（x 贴主街，市巷 y=26）；中央石板广场。
3. **tile 43 坊墙**（⚠️ 规划原文写 41，但 41 已被雪田占用——**deviation 已登记**）：城砖淡色版 `_ward_wall_tile`，tileset 注册+collision，minimap 配色，Main tile_files 补查。
4. **城门楼**：`gate_tower.png`（48×52 程序化双层楼子，generate_city_props 生成）——**纯视觉 Sprite**（z3 无碰撞不占格，挂四门上方 offset(0,-26)）。
5. **建筑 27 座**（bdefs 表驱动）：官署坊 yamen+constable、寺观坊 temple、西市 smithy/apothecary/cloth+摊×2+well、东市 grocery/house_ne+摊×2、副块 tavern+hut×2、西坊民居6/东坊民居5。**key 保持 npc_spawner 依赖**（tavern/apothecary/smithy/cloth/yamen/house_w/house_ne/stall_w1/stall_e1/well/plaza）。`city_info` v2：+gates 命名/wards[{name,rect}]/markets/buildings+job。
6. **门前小径 `_connect_door_to_road`**：door BFS 找最近路（path/35/17），阻挡 39/40/43/5，沿途铺 path。
7. **河流避城加固**（W2 新增教训）：
   - 城是方形——水距城判定必须用**切比雪夫距离**（欧氏放过城角内的水：城角距城心 30√2）
   - `_find_river_sources` **源点避城**（曾有源点落在城角禁域内→河出生即困死 1000 步）
   - 河选步：城墙邻域（cheby<half+2）**绝对拒绝**（score+=1000 无效——相对排序内仍会选禁域低地）
   - 兜底 8 邻径向外推 + `_force_sea_connection`（干流终态非湖/海时从尾端强制接海）
   - 湖泊避城 45→62（城外接圆+湖半径，旧值湖缘会侵入方形城角）
8. **探针升级**：wards_n/markets_n、坊内连通（中巷格从广场 BFS 可达=坊门有效）、房间距 **grow(1)** 不相交=间隙≥2（grow(2) 语义错对应≥4）；修复 town 循环覆盖 `binfo` 变量导致 city doors 空集假 PASS 的探针 bug。

### W2 基线数据锚点
 wards=4 / markets=2 / buildings=27 / 城内水 0 / 河距城(cheby) 36 / 房间距全过 / reach=90191 / 8 镇。

### ⚠️ W3 前须知
- 目检截图未做（旧探针 probe_biome_shots 已失效）——W3 开工前先按 `_climate_kind` 重写截图探针机位（城 4 机位：广场/官署/西市/东坊+领地预留）。
- 门派领地选址 `SECT_TERRITORIES` 就绪；距城 ≥40 断言用切比雪夫距离（同河教训）。
- `_compose_big_building` 接 STYLE（WorldData.SECT_TERRITORIES[x]["style"]）时注意 legacy accent 参数共存。

---

## 二·B、已完成（W1 气候地图，2026-08-31 二次会话）

**改动文件**：`scripts/world_generator.gd`（气候场/河流/装饰）、`tools/probe_world_regress.gd`、`tools/regress_world.py`（CURRENT_STAGE=W1）。**结果：26 断言 26 PASS 0 FAIL 0 PENDING。**

### 实现要点（与 §三计划的差异均已验证）
1. **气候场**：`_climate_temp`（纬度 0.04+lat*0.92 − 海拔*0.38 + 噪声±0.07 + 出生区<26格向0.55修正）、`_climate_hum`（噪声归一+临水+0.2）、`_climate_kind(x,y,jitter)` 判定链；`_biome_kind` 转发保持全项目接口；`_ground_of` 改为 temp±0.045 抖动判定（锯齿过渡替代 Voronoi dither，`_biome_nearest_two`/`BIOME_BLEND_BAND` 已删）。
2. **沙漠自然规律**：`t≥0.79 且 h−max(0,t−0.62)*1.6 < 0.30`（热带蒸发干旱；0.7~0.79 为 plains 缓冲带隔断 desert|forest 咬合）。
3. **河流重写**：源=高山群系高处（`_find_river_sources`，不足时海拔阈值 0.3→0.15 放宽）；`_walk_river` 8邻下坡+噪声+边界引导+城市**方形斥力**（河可贴墙南北绕行）+湖泊引力；干流 400 步未终湖海 → 径向 d4 冲海保底（无视 visited，上限 1000 步）；支流触碰水即并入。`_stamp_river` 干流水4格/支流2格+岸沙；`_bridge_along` 沿程稀疏基本桥（干流每36步/支流40步）保世界连通（W2 官道桥/W5 石拱桥再规则化）。`river_paths=[{path,main}]`、`lake_centers` 供断言采样。
4. **湖泊**：1~2 个显式湖（洼地 h<-0.2 + 暖带 + 避城45/镇20/互距60/避出生区），湖面 override=5+湖缘沙 6，湖种子登记 `biome_seeds`；lake 群系动态画水已删（只留湖缘装饰）。
5. **临水加湿**：`_build_water_humid_boost`（河中心线/湖心外扩 12 格，O(中心线)构建）。
6. **树密度=湿度**：`dens=clamp(hum*1.2,0.35,1)` 调制各群系树阈值；沙漠零树、雪原稀雪松、竹林由气候判定保证。
7. **连通性算法升级（重要教训）**：河无等距桥 → `_find_pockets` 4000 截断把巨 pocket 重复报告几十次 × 旧 `_carve_connection` O(pocket×reach)（reach.keys() 循环内反复复制 8 万级数组）→ 启动卡死 180s+。**修复**：_find_pockets 完整洪泛（每 pocket 只报一次）+ 新 `_carve_via_field`（一次性全图施工成本场 multi-source BFS，每 pocket 取场值最小格沿梯度开路，O(全图)）。
8. **探针升级**：群系代表区中心=**最深点**（±18 格采样邻域内异类最少的格；首见格在边缘、质心在月牙形 desert 弧内凹处，都曾导致直方图混入邻居群系）。desert 禁格断言改占比 ≤5%（残余为 _ground_of 锯齿过渡带设计行为，绝对零须关闭过渡带）。

### W1 基线数据锚点
7 群系采样格：snow 908 / desert 248 / lake 20 / bamboo 378 / forest 397 / plains 891 / mountain 1002；reach=91665；河 3 干流+2 支流；2 湖；8 镇；城内水 0；河距城 27.0。

### ⚠️ W2 前须知
- `tools/probe_biome_shots.gd`（旧截图探针）复刻的 Voronoi 雪首府公式已失效，目检截图前需按 `_climate_kind` 重写定位逻辑。
- W2 时 `CITY_HALF 22→30`，`regress_world.py` 的 `river_not_through_city` 阈值 23.0 需同步改 31.0。
- 连通性 pass 上限现为 4 轮，若 W2 城池扩大后出现 conn pass 打印到 3 轮以上需关注。

---

## 二、已完成（W0 地基，2026-08-31）

### 新增文件
| 文件 | 内容 |
|------|------|
| `scripts/world_features.gd` | 特性开关 `WorldFeatures.FLAG`（7 个 FLAG，当前全 false，仅 `quests_disabled=true`）+ `WORLD_RULES_VERSION=1` |
| `scripts/world_data.gd` | 常量数据表 `WorldData`：CITY_V2（唐制蓝图）、STYLE_CIVILIAN、SECT_TERRITORIES（5 派）、TOWN_TEMPLATES_V2（3 型）、NPC_JOBS（11 岗）、WALKABILITY——**一次建全，各阶段引用** |
| `tools/probe_world_regress.gd` | 游戏内采样探针（临时 autoload 注入）：群系直方图/气候邻接对/水距城镇/建筑压水/城内 BFS 四门+door 可达/出生点可达区 → 写 `tools/regress_world_data.json` |
| `tools/regress_world.py` | 回归 runner：注入→跑游戏→还原 project.godot→读 JSON→断言→PASS/PENDING/FAIL 报告（退出码 0/1/2） |

### 基线结果（stage=W0）
- **20 PASS / 2 PENDING / 0 FAIL**：
  - biome：7 群系存在性 ✓、雪原零绿瓦 ✓、雪原无橡树 ✓、沙漠无树无草 ✓、气候连续性（禁邻接对）✓
  - water：footprint 无压水 ✓、河不穿镇(min 14.0) ✓
  - city：四门可达 ✓、全部建筑 door_px 可达 ✓
  - walk：出生点可达区 100771 格 ✓
- **PENDING 2 条**（`river_not_through_city` min=15.8、`city_interior_dry` cells=105）：现状为第三轮已验收的"穿城河+水门街桥"语义（规划 §3.2 保留），断言标记 `since=W1`，河流改道后自动生效。

### 关键设计决策
1. **断言生效阶段机制**：`regress_world.py` 的 `check(..., since="W1")` + `CURRENT_STAGE`——历史语义豁免报 PENDING 不算 FAIL，避免"改断言凑绿"纪律与已验收成果冲突。
2. 探针只采样原始数据（JSON），断言全部在 python 侧，保证"断言失败先修因"时游戏侧无需改动。
3. `WorldData` 表按规划 §12 草案一次建全（含 Color 常量），W2-W6 直接引用不再改表结构。
4. 基线数据锚点：雪区 660 / 漠区 836 / 湖区 208 / 竹 394 / 林 414 / 平原 886 / 山 446（采样格），供 W1 重写后对比群系均衡度。

---

## 三、下一步：W1 气候地图实施要点（未写代码）

改动文件：仅 `scripts/world_generator.gd`。完成后 `regress_world.py` 将 `CURRENT_STAGE` 改 "W1" 跑全量。

### 1. 气候场替换 Voronoi（`_setup_biomes`/`_biome_kind`）
- 新增 `_climate_temp(x,y)`：纬度基线 `0.04 + lat*0.92`（y=-R→北冷，y=+R→南暖，lat=(y+R)/2R）+ 海拔修正 `-maxf(get_height,0)*0.38` + detail 噪声扰动 ±0.07；**出生区修正**：距原点<26 格向 temp=0.55 lerp（保出生区温和）。
- 新增 `_climate_hum(x,y)`：`get_humidity*0.5+0.5` + 临水加湿（`_water_humid_boost` 字典，见 §3）。
- `_climate_biome(x,y)` 判定（阈值）：t<0.25→snow；t<0.45→mountain；t<0.7→(hum≥0.62→bamboo；hum>0.45→forest；else plains)；t≥0.7→(hum<0.32→desert；else plains)；湖泊种子半径内→lake（沿用 dist<11+d*3 噪声缘）。
- **保留** `biome_seeds` 数组但语义变更：只存湖泊种子（`_generate_rivers` 生成湖时 append）+ 中央平原锚可删（出生区修正替代）。`_lake_surface_dist`/`get_terrain`/`_biome_decor_tile` 的 lake 分支继续读它。
- `_biome_kind` 变为 `_climate_biome` 的转发（保持全项目调用口不变）；`_biome_nearest_two` 保留但可简化（`_ground_of` 的 dither 过渡改为：判定前 temp 加 ±0.045 抖动 → 阈值边界天然锯齿化，替代 Voronoi 过渡带；`BIOME_BLEND_BAND` 相关逻辑可简化）。
- 注意 `_spawn_tree_prop` 里 `_biome_kind=="snow"` 染色、`_generate_towns` 群系避让、`BIOME_PALETTES` 均依赖群系字符串，接口不变即安全。

### 2. 河流改道（`_generate_rivers` 重写，规划 §5.1）
- 源点：2-3 个，在 `_climate_biome=="mountain"` 且 `get_height>0.3` 的分散高山点（种子 WORLD_SEED+3000）。
- 干流游走：每步在 8 邻域选 `get_height` 最低方向+detail 噪声摆动；**斥力项**：距城心 < CITY_HALF+6 时向远离城心方向偏置（W1 断言 river_not_through_city 生效的前提）；**终点**：距湖心<湖半径（汇入）或 length>WORLD_RADIUS-6（出海）；步数上限 600，超限直线冲向最近边界。
- 干流水面宽 4（垂直流向 w∈{-2,-1,0,1}）、支流 2-3；岸沙 1 格（`|w|` 外圈 6）；**水格全部写 override=5**（SSOT）。
- 支流：1-2 条，从其他源点游走，距干流水格≤2 时并入（沿干流继续可选）。
- 湖泊：1-2 个显式湖——选点 t>0.4、height<-0.25 洼地、距城≥45、距镇≥20、互距≥60、距原点≥30；半径 9~13；湖面 override=5 + 湖缘 6；湖心 append 进 `biome_seeds`（kind="lake"）。**删除 lake 群系的动态画水**（`_biome_decor_tile` lake 分支只留湖缘沙/花装饰）。
- 临水加湿表：湖/河生成完后构建 `_water_humid_boost: Dictionary`（遍历水格向外扩 12 格方形标记），构建成本约几百万次字典写属一次性可接受；`_climate_hum` O(1) 查询。
- **旧等距桥逻辑删除**（每隔 15 格架桥）：基本连通交给 `_ensure_connectivity` pocket 修补（水→桥 17 已有），官道桥在 W2、石拱桥在 W5。⚠️ 需关注 `river_not_through_town` 与城镇选址 `_area_has_water(12)` 的联动——城镇数可能下降，基线锚 6-9 个，若 <4 需放宽候选（河畔优先是 W4 的事，W1 保持现状窗口）。

### 3. 树密度=湿度（`_biome_decor_tile`）
- 各群系树阈值乘 `density_scale = clampf(hum*1.2, 0.35, 1.0)`（hum 用 `_climate_hum`）；沙漠树=0（岩石保留）；雪原仅雪松 4（已有冷霜染色）；竹林由气候判定保证 hum>0.62。

### 4. W1 新增断言（regress_world.py 增补，since=W1）
- 河源在山：干流前 10 格群系 ∈ {mountain, snow}（探针记录每条干流轨迹前 10 格群系）。
- 河终湖/海：干流末端格 length>WORLD_RADIUS-10 或 距湖心<湖半径+3（探针记录末端位置与湖心表）。
- 探针需新增采样：干流轨迹（`_generate_rivers` 时把轨迹存 `wg.river_paths: Array[PackedVector2Array]`）+ 湖心表 `wg.lake_centers`。

### 5. 验证流程（W1 完成判定）
1. `CURRENT_STAGE="W1"` 跑 `python tools/regress_world.py` → 0 FAIL（含新河源/河终断言）。
2. 目检：`tools/run_biome_shots.py` 9 机位截图（注意该脚本探针复刻 `_setup_biomes` 旧 Voronoi 会失效，可只看截图不走其断言）。
3. 更新本文档 + gameWork.md。

---

## 四、W2-W8 备忘（细读规划原文再动工）

- **W2 唐制城池**：`CITY_HALF 22→30`（61×61）；`WorldData.CITY_V2` 已备好坊/市矩形（§12 注明"以施工时微调为准"）；新 tile 41 坊墙+城门楼/坊门贴图（`texture_generator` 新函数 + 删 PNG 重生成纪律）；`city_info` v2 结构升级；`_force_place_building_prop` 沿用；水关=穿坊河道处坊墙改水门。
- **W3 门派领地**：`SECT_TERRITORIES` 已备；选址硬距离（领地间≥45/距城≥40/距镇≥20）；STYLE 传入 `_compose_big_building`（在 `texture_generator.gd`，目前散传 accent 参数需重构）；界碑环+山门牌坊新 prop；与 ClanSimulator 名册对齐。
- **W4 村镇+NPC**：`TOWN_TEMPLATES_V2`/`NPC_JOBS` 已备；`npc_spawner` 重写落位（只读 door_px+offset）；任务冻结接线：`GameManager` 读 `WorldFeatures.FLAG["quests_disabled"]` → `main_story._start_when_ready` return / `QuestSystem` 不发布 / `EncounterSystem` 关任务型奇遇 / `mob_spawner.spawn_story_camp` 不调用。**删除为 0，全走开关**。
- **W5 石拱桥**：`BridgeProp` prop（`texture_generator._bridge_stone(W)` 三档宽）；`_place_bridges` 规则化（官道×河必有桥）；tile 17 语义不变。
- **W6 碰撞治理**：`WorldData.WALKABILITY` 已备；SETTLEMENT/ROAD/FARM 禁碰撞装饰（在 `get_tile_id` 装饰分支前查分区）；岩石聚簇（贴山 3 格 2~5 连片）；`_ensure_connectivity` 升级 2×2 走廊 BFS；`is_walkable(tx,ty)` 统一查询口。
- **W7**：终验矩阵全绿 + 手动冒烟单（规划 §10.4）+ 旧路径函数删除（净删 ~600 行）。

## 五、操作指引（下次开窗继续）

1. 必读：本文件 + 规划文档对应阶段章节 + `docs/开发必读-陷阱备忘.md`。
2. W1 开工：按 §三要点改 `world_generator.gd`（一次只动一个 FLAG 域，W1 无新 FLAG，直接改）。
3. 验证：`python tools/regress_world.py`（先改 `CURRENT_STAGE="W1"`）。
4. 每阶段收尾：更新本文件 + `.trae/gameWork.md`（Get-Date 取真实日期）。
