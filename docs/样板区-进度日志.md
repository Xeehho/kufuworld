# 城镇样板区进度日志（跨会话续接用）

> 总方案见 [docs/立项-城镇样板区重构.md](立项-城镇样板区重构.md)。本文件记录每阶段执行状态，
> **每完成一小步立即更新**。新会话续接时：先读本文件"当前状态"节 + 立项书，再继续未完成项。

---

## 当前状态（新会话从这里接手）

- **P0~P3 全部完成（2026-09-02）**，回归 102 PASS / 0 FAIL；截图 demo_p0/p1/p2/p3_*.png
  （p3：farm_a/farm_b 菜圃特写 + yard_house/yard_barn 前院 + fence_s 南带围栏全景）
- 实测选址（seed=12345）：**rect=[P:(15,43) S:(56,44)]，中心 (43,65)，距出生点 (49,23) 42 格**；
  区口 gate_in=(67,43)；入口路=官道(75,43)→区口 8 格；平整山/水 309 格（13.4%）
- **P3 院落菜圃（全局坐标）**：
  - 6 建筑地块：门径向南接巷（path）+ 前院碎石平台 3x2 + bounds 一圈围栏（南缘 door.x±1 留口）
  - 2 菜圃（farm_a (20,71)-(33,80) / farm_b (38,71)-(49,80)）：垄行农田瓦 16（内缩1 行距2，override
    在 chunks 前）+ 垄格 crop 精灵 70%（detail_noise 确定性）+ 稻草人（地块中心）+ 棚架x2（对角）
    + 围栏（北缘 door.x±1 留口）
  - 围栏=32px 双格单元（demo_fence：3柱双横杆；横排底缘锚定/竖排格中心+90°旋转），prop 数减半
    保 ≤400 预算（实测总 prop ~280）
- **P2 建筑落位（全局瓦片 anchor，fp=footprint）**：
  - barn=demo_barn@(21,45) fp6x5 / well_plaza=demo_well@(42,50) / greenhouse=demo_green@(58,47) fp5x4
  - house_a=demo_house@(22,60) / longhouse=demo_long@(35,60) fp6x4 / house_b=demo_house@(49,60) /
    shop=demo_shop@(60,60)——落位后 plot.door 重算=建筑底缘中点（门朝南）
- **P2 纹理管线**：`texture_generator.generate_demo_buildings()`（Main._ensure_textures 接线，
  检测 demo_house **或 demo_fence** 任一缺失→幂等重生成）→ `_compose_demo_house(W,H,style)`：
  style=teal（民居青瓦）/red（谷仓·商铺红棕瓦，谷仓大门22px+干草方格窗，商铺出檐+货台）/
  glass（温室玻璃格顶）；四要素=梯形瓦面(_roof_pat 调制)+石砌烟囱+板门双侧木框玻璃窗+台基台阶
- **下一阶段 P4 装点与密度**：_scatter_decor 植被/道具散布（装饰密度≥25%、道具每8×8≥1组、
  3×3 单色连片=0）、果树带（北带疏林）、夜景灯笼检查；遗留：谷仓周边沙色地面（mountain 过渡）
  靠 P4 碎屑缓解、入口路 P0 版断续并行线重铺
- **下一阶段 P5 终验**：8 机位漫游 vs 材质包 2 样图逐项走查（立项书 §七 清单）+ regress 全绿

## 关键事实速查（已核实，避免重复调查）

- **⚠️ build 顺序契约（P0 踩坑）**：override 写入（平整/铺路）**必须先于 `_load_zone_chunks`**——
  override 上漆只发生在 `_load_chunk` 一次性执行，后写不上屏（首版入口路因此隐形，已修）
- **选址否决分布实测（半径150/1963候选）**：hard(山611/水253/雪山147/39建筑123) 1168、city 399、
  border 373、clear 15、road_band 6——"零山零水 56×44 净地"全图不存在，故 GEO_TILES(3/5/7)
  允许 ≤15% 并在 `_flatten_ground` 平整为群系地面（呼应立项书 §三"接现状水域/自挖池塘"）；
  人工领地瓦片 BUILD_TILES 零容忍
- **TownDemoKit 常量/方法**（`scripts/town_demo_kit.gd`）：
  - `wg` = WorldGenerator 引用（无类型）；`rect: Rect2i` 选定区；`gate_in: Vector2i` 区口内侧
  - `setup(world_gen) -> bool`：网格扫描选址（必须先于 build）；`build()`：登记+骨架
  - `_site_reason` 返回否决原因码（""=过）：A 界/城剪枝 → B rect 内步长2 扫描（BUILD 一票否决 +
    GEO ≤15% + 植被 ≤26%）→ C 外扩2框带官道set/净空全扫；`_gate_reachable`：官道最近格距 rect ≤55
  - `_stamp_prop(png, cell)`：脚底锚定 z=2 无碰撞 prop（挂 demo_kit 自身，World 递归 Y-sort 吸收）
  - `_pave(c)`：`wg.override_cells[c] = wg._palette_at(x,y)["path"]`（plains=瓦片1）
- **回归/探针契约**：
  - regress 102 断言对样板区零覆盖；`mob/camp_*_wild` 断言要求营地 `is_in_settlement=False`
    ——样板区纳入该判定后营地会被外推出区，断言仍绿（2026-09-02 实测 PASS）
  - 选址网格扫描确定性（无随机），但改动 `town_demo_kit.gd` 后必须重跑 regress
- **世界实测参数（seed=12345，勿再猜）**：城 (75,0) **half=30**（city_half 变量，勿用 22）；
  出生点 (49,23)；官道 4 条各 len=96；河 3 主 2 支 + 湖 2；镇 7 个全在西/南远处（cheby>100）；
  门派 5 个：青峰(-124,-26)r28 / 铁砂(18,151)r24 / 古刹(117,97)r26 / 药王(167,0)r24 / 幽冥(-91,-112)r28
- **立项书 §2.2 勘误**：城半边 22→实为 30（城 clearance 方 71×71 占据出生点东北），
  "半径 40 内扫 56×44"不可行；网格扫描取代随机撒点（确定性+完备性），山水平整取代零山水
- 瓦片 16×16；prop 脚底锚定= `pos=(x*16+8, (y+1)*16-2)` + `offset=(0,-h/2)`
- 纹理可复用清单（`res://sprites/buildings/`）：lantern/barrel/crate/scarecrow/fence_seg/well/
  stall_red/stall_teal（缺失时 TextureGen.generate_city_props 补生成）
- 入口路 BFS：阻挡=BUILD+GEO+SOFT（树/石也绕），框=外接方框+60，q 上限 16000；接入点排除桥格(17)
- **截图工具**：`python tools/run_demo_shots.py`（4 机位，probe_demo_shots.gd 注入式，机位表随选址变更要同步）

## P0 立项与骨架（2026-09-02 完成）

| 步骤 | 内容 | 状态 | 产出/备注 |
|------|------|------|-----------|
| P0.1 模块骨架 | `town_demo_kit.gd`（preload 引入，避免 class_name 缓存假错）；TextureGen 走项目惯例 `const X = preload()` | ✅ | scripts/town_demo_kit.gd |
| P0.2 选址 | 三轮迭代：随机撒点(40/60/80×300)无解 → 网格扫描(步长6/半径150)+距离排序 → "零山水"不可行(否决分布实测) → GEO≤15% 平整方案 | ✅ | rect=(15,43)+(56,44)，否决分布见关键事实 |
| P0.3 登记制 | `wg.demo_zone_rect` + `is_in_settlement` 纳入（营地/野怪避让）；`_register_town_clearance(half=max(w,h)/2+1)` 复用镇净空；chunk 预载 42 | ✅ | world_generator +3 行 |
| P0.4 山水平整 | `_flatten_ground`：区内 GEO_TILES(3/5/7)→群系地面 override，309 格（13.4%）；可达性只增不减 | ✅ | 立项书未预见的必要偏差，见"立项书勘误" |
| P0.5 入口路 | 官道(75,43)→区口(67,43) BFS 绕障碍，2 宽 path（主线+法向1格）；**踩坑：铺路先于 chunk 预载**（override 上漆一次性） | ✅ | 首版隐形已修 |
| P0.6 边界+立牌 | 灯柱×8（四角+四边中点）；区口旁 "· 样 板 村 ·" Label（font5 金字描边 z=20）+ 灯柱 | ✅ | 纯视觉零语义 |
| P0.7 回归 | regress_world.py **102 PASS / 0 FAIL**（最终代码状态下复跑确认）；DemoKit 日志五行入 regress_world_log.txt | ✅ | tools/regress_p0_final.txt |
| P0.8 截图验证 | `run_demo_shots.py` 4 机位入库 docs/shots/demo_p0_*.png；走查：立牌/灯柱/入口路/平整/净空全 ✓ | ✅ | 角机位灯柱与玩家同格重叠非缺陷 |

## P1 路网与地块（2026-09-02 完成）

| 步骤 | 内容 | 状态 | 产出/备注 |
|------|------|------|-----------|
| P1.1 地块划分 | `_stamp_plots()` 记录制 9 地块（kind/bounds/door 全局坐标表驱动）；门位全贴巷（door 4 格内有巷 100%） | ✅ | plots 数组供 P2/P3 消费 |
| P1.2 主巷 | 3 段大弯 2 宽（弯幅 3 格）+竖向过渡——首版 4 段仅 1 格错位"近直线"不达样图走势，改大弯后全景走查 ✓ | ✅ | 竖过渡 2 宽 x21,22/39,40 |
| P1.3 支巷 | 纵巷×3（地块间隙 x=15/29/41）+横巷 y=27 通菜圃；谷仓门 (9,9) 即巷 | ✅ | 1 宽 |
| P1.4 广场 | 水井广场石板 35（8×8），南缘邻主巷 | ✅ | P3 落水井 |
| P1.5 回归+截图 | 102 PASS / 0 FAIL；p1_overview 全景（zoom1.1）走查：三级路网/广场/官道桥衔接全 ✓ | ✅ | probe_demo_shots.gd 支持 zoom 机位 |

## P2 建筑 Prefab（2026-09-02 完成）

| 步骤 | 内容 | 状态 | 产出/备注 |
|------|------|------|-----------|
| P2.1 纹理合成器 | `_compose_demo_house(W,H,style)`：梯形瓦面（_roof_pat 调制/玻璃格）+石砌烟囱+木框抹灰墙（顶梁/角柱/竖柱/V斜撑）+板门+木框玻璃窗+窗台石+台基台阶；barn 大门22px+干草窗、shop 出檐+货台、green 玻璃格顶 | ✅ | texture_generator.gd 尾部 |
| P2.2 生成接线 | `generate_demo_buildings()` → sprites/demo/ 5 张 PNG（缺失才生成）；Main._ensure_textures 补 demo 检测 | ✅ | 实测 5 张 3.7~8.3KB |
| P2.3 落位 | `_place_buildings()` 锚点表 7 栋（含水井复用 buildings/well.png）；plot.door 重算=底缘中点；`_stamp_building`（Sprite z2 底缘锚定+墙脚影+StaticBody 不占格） | ✅ | demo_building 组 |
| P2.4 檐下道具 | 每栋左桶右箱（feet_y=底缘行） | ✅ | barrel/crate |
| P2.5 回归+特写 | 102 PASS / 0 FAIL；7 机位走查：**四要素每栋齐全**，谷仓干草窗/长屋三窗/温室玻璃格/商铺出檐货台/水井井架全在位 | ✅ | 探针 zoom 每机位显式设置（修 zoom 继承 bug） |

## P3 院落与菜圃（2026-09-02 完成）

| 步骤 | 内容 | 状态 | 产出/备注 |
|------|------|------|-----------|
| P3.1 配套纹理 | demo_fence（32px 双格：3柱双横杆，prop 数减半控预算）+ demo_trellis（棚架）；generate_demo_buildings 尾部 extras 生成 | ✅ | _fence_unit_img/_trellis_img |
| P3.2 菜圃 | `_stamp_farm_ridges`（垄行瓦16 override，**chunks 前**）+ `_build_farm`（crop 70% 噪声撒+稻草人+棚架x2+围栏北门口） | ✅ | 行距2"作物成行" |
| P3.3 前院 | `_build_yard`：门径 door→南缘接巷 + 碎石平台 3x2 + 围栏（南缘 door±1 留口） | ✅ | 6 地块 |
| P3.4 围栏系统 | `_fence_ring`（横边步2/竖边步2，角点横边覆盖）+ `_stamp_fence`（横排底缘锚定/竖排中心+90°旋转） | ✅ | 总 prop ~280<400 |
| P3.5 回归+走查 | 102 PASS / 0 FAIL；菜圃垄行/围栏/稻草人/棚架/南门留口/门径全 ✓（DoD：无裸地） | ✅ | **踩坑**：Main 检测粒度（只查 demo_house→fence/trellis 未生成），改检测 demo_house **或** demo_fence |

## P4 装点与密度（未开始）

- [ ] `_scatter_decor`：植被/道具散布至密度标准（§1.2 表：装饰≥25%、道具每8×8≥1组）
- [ ] 3×3 单色连片=0 检查；夜景灯笼亮度检查

## P5 终验（未开始）

- [ ] 漫游 8 机位 vs 材质包 2 样图逐项走查打勾（立项书 §七 清单）
- [ ] 用户验收签字 → 触发 v4 全量重构立项评审
